# @gyo-framework/sms-sender

## 0.1.0

- Initial release
- `send({ phoneNumber, message })` — send SMS via SmsManager
- `read({ limit })` — read recent SMS from inbox
- Android `SmsSenderBridge` Kotlin handler (SmsManager + ContentProvider)
