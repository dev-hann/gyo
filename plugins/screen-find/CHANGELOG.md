# @gyo-framework/screen-find

## 0.1.0

- Initial release
- `findByText({ text, exact })` — find UI elements by text content
- `findById({ id })` — find UI elements by view ID
- Android `ScreenFindBridge` Kotlin handler (AccessibilityService)
