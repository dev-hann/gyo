# Camera Demo Example

A Gyo app demonstrating the Camera plugin functionality.

## Getting Started

```bash
# Install dependencies
cd lib && npm install

# Run on Android
cd .. && gyo run android

# Run on iOS
cd .. && gyo run ios
```

## Features Demonstrated

- Take photos with device camera
- Pick images from gallery
- Display captured images
- Quality settings
- Error handling
