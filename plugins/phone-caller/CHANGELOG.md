# @gyo-framework/phone-caller

## 0.1.0

- Initial release
- `call({ phoneNumber })` — make phone call via ACTION_CALL intent
- `getCallLog({ limit })` — query CallLog.Calls content provider
- Android `PhoneCallerBridge` Kotlin handler (Telephony/CallLog)
